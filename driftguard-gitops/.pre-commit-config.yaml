repos:
  - repo: local
    hooks:
      - id: no-plaintext-secrets
        name: Reject plaintext secrets
        entry: python scripts/scan_no_plaintext_secrets.py .
        language: system
        pass_filenames: false
