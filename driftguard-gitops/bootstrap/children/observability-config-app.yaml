apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: driftguard-observability-config
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "25"
spec:
  project: observability
  source:
    repoURL: https://github.com/your-org/driftguard-gitops.git
    targetRevision: main
    path: observability
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      selfHeal: true
      prune: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
